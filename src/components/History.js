import React from 'react'
import BackNav from './BackNav'

const History = ({ user }) => {
    return (
        <>
            <BackNav link={'/dashboard'} />
        </>
    )
}

export default History
