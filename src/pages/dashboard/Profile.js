import React from 'react'
import NavBar from '../../components/NavBar'

const Profile = ({ user }) => {
    return (
        <>
            <NavBar user={user} />
        </>
    )
}

export default Profile
